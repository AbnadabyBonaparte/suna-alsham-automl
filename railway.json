{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install psutil redis aiohttp autopep8 && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python main_complete_system_v2.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "healthcheck": {
    "type": "http",
    "path": "/health",
    "port": 8080,
    "timeout": 30
  }
}
